import { InicialesPalabrasPipe } from './iniciales-palabras.pipe';

describe('InicialesPalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new InicialesPalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
